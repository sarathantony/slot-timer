{
  "extends": "./tsconfig.json",
  "compilerOptions": {
      "declaration": true
  },
  "exclude": [
    "__tests__",
    "dist", // Exclude the build output directory
    "**/*.spec.ts",
    "**/*.test.*"
  ]
}